#include<iostream>
#include<vector>
#include<algorithm>


typedef long long ll;

using namespace std;

int main(int argc, char const *argv[])
{
    
    ll test;
    cin>>test;
    while(test--){
       int n,c;
        cin>>n>>c;
        vector<int> v(n);
        for(int i=0;i<n;i++){
            cin>>v[i];
        }
        vector<int> ans(n,0);
        
    }
    return 0;
}
